# Supabase (Postgres + Auth + Storage) — see docs/tech-stack.md
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Prisma reads this directly (see prisma.config.ts) — use the Supabase
# connection-pooling URL, not the direct connection, for serverless deploys.
DATABASE_URL=

# Payment gateway — provider unconfirmed, see docs/tech-stack.md.
# Placeholder only until Phase 0's open decision is settled.
PAYMENT_GATEWAY_API_KEY=
